use purchase 
go
create database purchase
create table date_dimension(
day_number int primary key,
day_date date,
week_number int,
month_number int,
year_number int);

create table data_warehouse_facts(
fact_id int primary key,
customer_id int,
day_number int,
geographic_area int,
product_code int,
fact_name char,
fact_description varchar(50),
fact_amount int,
fact_count int,
orher_fact_details varchar(50));

create table customer_dimension(
customer_id int primary key,
customer_name char,
customer_address char,
other_details varchar(50))

create table product_dimension(
prodcut_code int primary key,
product_name char,
product_description varchar(50),
other_product_details varchar(50));

create table geographic_dimension(
geographic_area int primary key,
geographic_area_name char,
region char,
country char,
other_region_details varchar(50));











